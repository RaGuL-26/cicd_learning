name : cicd_lelevecho

on:
  workflow_dispatch:

jobs:
  java_code:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: |
          javac javas/hello.java
          java -cp javas hello

  run_py_code:
    runs-on: ubuntu-latest
    steps:
      - uses : actions/checkout@v4

      - name : python-repo
        run : |
          python RaGuL-26/pyhtoncicd/python.py
