name : ifcicd

on:
  workflow_dispatch:

jobs:
  job1:
    env :
      abcd : 1
    runs-on : ubuntu-latest
    steps:
      - uses : actions/checkout@v4
      
      - if : ${{ env.abcd == 1 }}   
        run : echo "abbbdd"
      - if : ${{ env.abcd != 1 }}
        run : echo wrong
      - run : echo ${{ env.abcd}}
